extends layout

block content

	h1.page-title Login

	.container
		form(action="/login", method="post")
			.form-group
				input.form-control(type="text", placeholder="login", name="user")
			.form-group
				input.form-control(type="passowrd", placeholder="password", name="pass")
			.form-group.text-center
				button.btn.btn-primary.btn-lg(type="submit") Login
