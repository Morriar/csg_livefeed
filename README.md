# CSG Competition Live Feed

Displays live feeds of competitions cams.

	sudo make install-deps
	make install
	make resetdata
	make run

Server will be ready at http://localhost:3000/.

Admin at http://localhost:3000/admin.
